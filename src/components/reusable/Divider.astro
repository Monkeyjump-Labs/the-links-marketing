---
interface Props {
  color?: 'softBlack' | 'offWhite';
}
const { color = 'softBlack' } = Astro.props;
---

<hr class:list={['m-0 border-0 border-t-2', color === 'offWhite' ? 'border-offWhite' : 'border-softBlack']} />
