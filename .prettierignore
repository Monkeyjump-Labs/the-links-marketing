# prettier-plugin-astro can't parse <script is:inline> inside {expression} blocks
src/layouts/BaseLayout.astro

# GENERATED by scripts/build-tokens.mjs, and byte-compared by `npm run tokens:check`.
# Prettier lowercases the hex values and rewraps the long lines, which makes the
# committed file differ from the compiler's output and fails the CI gate on a
# file nobody edited. The generator owns this file's formatting.
src/styles/tokens.css
